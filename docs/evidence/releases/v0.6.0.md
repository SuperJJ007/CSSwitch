# CSSwitch v0.6.0 发布证据

复核时间：2026-07-16（Asia/Shanghai）。本文件区分源码闸门、发布者人工验收、最终 artifact、Acceptance 安装与公开分发。

## Source 与公开发布

| 项目 | 结果 |
|---|---|
| 发布时 `origin/main` | `70950f1d1e8a18e8bd43db9b9a45e4e22ef2bcb4` |
| annotated tag object | `a8a2d8973315271220d539822d333c3fb5dad12f` |
| peeled `v0.6.0^{}` | `70950f1d1e8a18e8bd43db9b9a45e4e22ef2bcb4` |
| tag 签名 | unsigned annotated tag |
| 版本 / 架构 | `v0.6.0` / macOS Apple Silicon |
| GitHub Release | `CSSwitch v0.6.0`，非 draft、非 prerelease |
| 发布时间 | `2026-07-15T18:46:29Z` |
| 页面 | `https://github.com/SuperJJ007/CSSwitch/releases/tag/v0.6.0` |

Release 的 `targetCommitish` 为 `main`；最终 source identity 以上述公开 peeled tag 为准，不能用分支名、`targetCommitish` 或预合并构建替代。

## 自动化与代码审查

候选代码与 `origin/main@aa2cd34063f75e06f29cb7ce2fabe4208d4afcc9` 合并后，在 `435d8b9a57e668026b70aa4b20fcc4e51e84deb2` 和最终候选 `32aa062f22665163efa255cc8c98d26b43c89f09` 分别运行：

```text
bash test/run_all.sh --require-release-ready
```

结果退出 0：offline、loopback、scripts、rust、frontend 五层全部 `pass`，汇总为 `current-env clean: YES` 与 `release-ready green: YES`。其中 Tauri 为 232 passed / 3 explicit ignored，Gateway 为 102 passed。ignored 项是需要显式启动的隔离 runtime / real-Science E2E，不计作自动通过。

`git diff --quiet 32aa062 70950f1` 确认最终 PR merge commit 与已跑总闸门的候选具有相同源码树；merge 只改变提交身份。此前同一功能分支还分别完成 Gateway、Tauri、`skill-package`（42 passed / 4 explicit ignored）、bridge/runtime Python 边界测试，三个 Rust crate 的 `cargo clippy -- -D warnings`，以及 GitHub bundle、同提交复用、网络失败、无效提交、本地包和 bundle 卸载确认的定向验证。

## 发布者人工验收

发布者在 2026-07-16 报告“大部分验收成功”。这提供了继续合并发布的人工授权，但没有逐项结构化日志，因此只记作部分真机验收，不扩写成完整矩阵全部通过。本轮验收清单覆盖真实 Science / 千问交互、固定 Nature URL 安装与同提交复用、bundle 取消确认、停止后恢复以及本地包导入路线；发布者没有逐项说明其中哪些属于尚未完成的少数项。最终对外结论仍以发布者实际操作和公开 artifact 身份分别判断。

## 最终附件身份

| 项目 | 值 |
|---|---|
| 本地最终 DMG 路径 | `desktop/src-tauri/target/release/bundle/dmg/CSSwitch_0.6.0_aarch64.dmg` |
| 本地大小 | 9,701,904 bytes |
| 本地 SHA-256 | `4a67973ef090e6d0a0dbc8e85d456c81d87733831555f6ea84cecb75e563dfb4` |
| GitHub asset | `CSSwitch_0.6.0_aarch64.dmg`，状态 `uploaded`，content type `application/x-apple-diskimage` |
| GitHub size / digest | 9,701,904 bytes / `sha256:4a67973ef090e6d0a0dbc8e85d456c81d87733831555f6ea84cecb75e563dfb4` |
| 重新下载复核 | 与本地最终 DMG `cmp` 逐字节一致；SHA-256 相同；`hdiutil verify` 为 `VALID` |

最终 DMG 从 detached clean worktree 的 `70950f1` 重建。预合并 DMG 不作为发布附件，也不以其 hash 证明最终 release。只读挂载后，包内 app 为 `0.6.0` / `com.csswitch.menubar`，desktop 与 gateway 均为 arm64；两个二进制与构建目录中的 app 逐字节一致，包含外部 Skill route 字符串，不含旧 `Contents/Resources/proxy`。

## Acceptance 安装状态

从同一 `70950f1` 使用 `test/tauri.real-machine.conf.json` 构建独立 `com.csswitch.acceptance` 包。旧 Acceptance 先经正常应用退出流程停止，没有强杀进程；新包只替换 `/Applications/CSSwitch Acceptance.app`，安装前后均通过 `codesign --verify --deep --strict`，安装后二进制与该 Acceptance 构建产物逐字节一致，版本为 `0.6.0` 且无旧 proxy 资源。随后重新打开 UI；没有自动启动 Gateway。正式 `/Applications/CSSwitch.app` 未修改。

## 签名、notarization 与 Gatekeeper

| 检查 | 结果 | 准确含义 |
|---|---|---|
| `codesign --verify --deep --strict --verbose=4 CSSwitch.app` | 通过 | app seal / 内嵌签名结构有效 |
| `codesign -dvvv CSSwitch.app` | `Signature=adhoc`, `TeamIdentifier=not set` | 不是 Developer ID 团队签名 |
| `hdiutil verify CSSwitch_0.6.0_aarch64.dmg` | `VALID` | 磁盘镜像结构和内部校验有效 |
| `spctl --assess --type execute ... CSSwitch.app` | exit 3，`rejected` | Gatekeeper 未接受该 ad-hoc app |
| `xcrun stapler validate ...dmg` | exit 65，`does not have a ticket stapled` | 没有 stapled notarization ticket |

构建日志也明确跳过 notarization。结论：v0.6.0 是 ad-hoc signed、未建立 Developer ID / Apple notarization / stapled ticket / Gatekeeper acceptance 的公开附件。ad-hoc seal 验证通过不等于已签名公证。

## 尚未建立或不在本次声明中的层

- Developer ID 签名、Apple notarization、stapled ticket 与 Gatekeeper acceptance；这些已确认不存在，不是“待推断”；
- 所有 provider、所有 Skill 领域功能和真实 SSH server 的穷举验证；
- 未由发布者逐项记录的完整真机验收矩阵；
- 私有仓库、覆盖更新、部分 bundle 物理删除、Skill Manager / catalog / inventory / SQLite 集成。
